<%= form_for(@new_user, url: new_booking_path) do |f| %>

  <% if @new_user.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@new_user.errors.count, "error") %> prohibited
        this new_user from being saved:
      </h2>
      <ul>
        <% @new_user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <form class="form-inline">
    <div class="form-group">
      <%= f.label :name, 'Full Name' %>
      <%= f.text_field :name, class: 'form-control', required: true %>
    </div>

    <div class="form-group">
      <%= f.label :contact_number %>
      <%= f.telephone_field :contact_number, class: 'form-control', required: true%>
    </div>

    <div class="form-group">
      <%= f.label :email %>
      <%= f.email_field :email, class: 'form-control', type:'email', required: true %>
    </div>
  </form>
<% end %>
